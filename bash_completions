[[ -s ~/.git-completion.bash ]] && source ~/.git-completion.bash
[[ -s ~/.autojump/etc/profile.d/autojump.sh ]] && source ~/.autojump/etc/profile.d/autojump.sh
if command -v kubectl -h &> /dev/null
then
    source <(kubectl completion bash)
fi
